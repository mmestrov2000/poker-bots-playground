# Python Bot Example

This is the recommended starting point for the current platform runtime.

Files:
- `bot.json`
- `bot.py`

Manifest command:

```json
["python", "bot.py"]
```

Use this folder if you want the simplest and safest path from local testing to upload.
