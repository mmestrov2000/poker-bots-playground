services:
  app:
    build: .
    environment:
      - APP_ASSET_VERSION=${APP_ASSET_VERSION:-dev}
      - APP_AUTH_DB_PATH=/app/runtime/auth.sqlite3
    ports:
      - "8000:8000"
    volumes:
      - ./runtime:/app/runtime
    restart: unless-stopped
